# A CRLF checkout would make these unrunnable on the Linux runner, and it
# silently broke the test that parsed the workflow. Pin them.
*.sh text eol=lf
*.mjs text eol=lf
*.yml text eol=lf
*.json text eol=lf
