# Bypass Jekyll — this is a plain static site (index.html + data.js + data/*.json).
# Without this file, GitHub Pages runs Jekyll on the repo and fails because the
# inline JS template literals (${...}) and other content confuse the Liquid parser.
